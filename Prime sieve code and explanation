#include <bits/stdc++.h>
using namespace std;

int main() {
   //program to print all prime numbers till n using  prime sieve concept 
   int n;
   cin>>n;
   int sieve[n+1]={0};
   for(int i = 2;i<=n;i++){
       if(sieve[i]==0){
           for(int j = i*i;j<=n;j+=i){
               if(sieve[j]==0){
                   sieve[j] = 1;
               }
           }
       }
   }
   
   for(int i = 2;i<=n;i++){
       if(sieve[i]==0){
           cout<<i<<" ";
       }
   }
}
